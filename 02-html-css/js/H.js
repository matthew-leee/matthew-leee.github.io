function calculator (str, num1, num2) {
    if (str == '+'){
        return num1 + num2
    }; else (str == '-'){
        return num1 + num2
    }; else (str == '*'){
        return num1 * num2
    }; else (str == '/'){
        return num1 / num2
    };